# QUANTOMDOCS ENVIRONMENT CONFIGURATION
# Copy this file to .env and update with your values
# DO NOT commit .env file to version control!

# ==========================================
# APPLICATION SETTINGS
# ==========================================

# Node environment (development, production, test)
NODE_ENV=production

# Server configuration
PORT=5005
HOST=0.0.0.0

# ==========================================
# SECURITY SETTINGS (REQUIRED!)
# ==========================================

# JWT Secret Key - MUST be changed in production!
# Generate a secure random string (minimum 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=your-secret-key-here

# ==========================================
# OPTIONAL SETTINGS
# ==========================================

# Logging level (error, warn, info, debug)
# LOG_LEVEL=info

# Session timeout in seconds (default: 3600 = 1 hour)
# SESSION_TIMEOUT=3600

# Maximum file upload size in MB (default: 10)
# MAX_UPLOAD_SIZE=10

# ==========================================
# PRODUCTION DEPLOYMENT NOTES
# ==========================================
#
# 1. SECURITY:
#    - Always set a strong, unique JWT_SECRET in production
#    - Never use default or example values
#    - Keep this file secure and never commit it to Git
#
# 2. DOCKER DEPLOYMENT:
#    - These values can be overridden by environment variables in docker-compose.yml
#    - Or set them directly in your hosting environment (Railway, Heroku, etc.)
#
# 3. REQUIRED VARIABLES:
#    - JWT_SECRET: Required for authentication (CRITICAL!)
#    - NODE_ENV: Should be "production" for live deployments
#    - PORT: Server port (default: 5005)
#    - HOST: Server host (default: 0.0.0.0)
#
# 4. GENERATING SECURE SECRETS:
#    - Linux/Mac: openssl rand -base64 32
#    - Node.js: require('crypto').randomBytes(32).toString('base64')
#    - Online: use a password generator for 32+ characters
